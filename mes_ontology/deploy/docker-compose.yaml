# deploy 경로로 실행:
#   docker compose -f deploy/docker-compose.yaml up -d
# (mes_ontology 안에서 실행하거나, -f 에 절대 경로를 주면 됩니다.)
#
# Compose v2.20 미만이면 include 미지원 → 루트의 compose.yaml 을 직접 지정:
#   docker compose -f compose.yaml up -d

include:
  - path: ../compose.yaml
